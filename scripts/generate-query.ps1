# Based on the template structure
# Language -> Table -> Category
# when (Language) set variable
# when (Table) create new table / update existing
# when (Category) insert row into the current table
# different modes: one for purging and restarting from scratch
#   another for only creating initial tables
#   another for only adding rows to existing
